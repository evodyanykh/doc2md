"""
Центральная конфигурация проекта.
Все настройки меняются только здесь.
"""

# ─── Поддерживаемые форматы ───────────────────────────────────────────────────

# Расширения файлов, которые будут обработаны при рекурсивном обходе папок
SUPPORTED_EXTENSIONS = {".docx", ".pptx", ".pdf"}

# ─── OCR ──────────────────────────────────────────────────────────────────────

# Язык для Tesseract OCR.
# Для русского: "rus", для русского + английского: "rus+eng"
OCR_LANGUAGE = "rus+eng"

# Путь к исполняемому файлу Tesseract.
# Windows: укажи свой путь после установки Tesseract.
# Пример: r"C:\Program Files\Tesseract-OCR\tesseract.exe"
# Если Tesseract добавлен в PATH — можно оставить None.
TESSERACT_CMD = r"C:\Program Files\Tesseract-OCR\tesseract.exe"

# DPI для рендеринга страниц PDF перед OCR (300 — стандарт для хорошего качества)
PDF_OCR_DPI = 300

# Минимальное количество символов на странице PDF,
# ниже которого страница считается сканом и отправляется на OCR
PDF_MIN_CHARS_FOR_TEXT = 50

# ─── Версионирование ──────────────────────────────────────────────────────────

# Суффикс версии в имени выходного .md файла.
# Итоговое имя: document_v1.md, document_v2.md и т.д.
VERSION_SUFFIX = "_v"

# ─── Логирование ──────────────────────────────────────────────────────────────

# Имя файла с логом ошибок (создаётся рядом с запускаемым скриптом)
LOG_FILE = "errors.log"

# Имя итогового отчёта о конвертации
REPORT_FILE = "report.md"

# ─── Маркеры в выходном .md ───────────────────────────────────────────────────

# Вставляется вместо изображений без текста
IMAGE_PLACEHOLDER = "[⚠️ ИЗОБРАЖЕНИЕ — требует проверки]"

# Вставляется перед потенциально некорректно распознанными таблицами
TABLE_WARNING = "[⚠️ ТАБЛИЦА — возможно некорректный парсинг]"

# Вставляется, если OCR не дал результата
OCR_FAILED_PLACEHOLDER = "[⚠️ OCR НЕ РАСПОЗНАЛ ТЕКСТ — требует ручного ввода]"
